<input
    type="button"
    class="{$inputDetails.class} btn"
    style="{$inputDetails.style}"
    name="{$inputDetails.name}"
    id="{$inputDetails.id}"
    value="{$inputDetails.value}"
    accesskey="{$inputDetails.access}"
    />